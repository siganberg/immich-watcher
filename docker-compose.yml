name: immich-wather

services:
  server:
    image: immich-watcher:dev 
    container_name: immich_wather
    environment:
      - IMMICH_HOST=https://photos.marasigan.net
      - IMMICH_API_KEY=${IMMICH_API_KEY}
    restart: always
    volumes:
      - /Users/francis.marasigan/immich-watcher:/var/lib/data
